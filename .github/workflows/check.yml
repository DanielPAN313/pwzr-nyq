name: Check

on:
  push:
    branches: [main, feature-miniprogram-flow, ui-polish, ui-polish-refresh, release-wechat-config]
  pull_request:
  workflow_dispatch:

jobs:
  miniprogram-preview:
    name: Mini Program preview checks (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run check

  ui-branch-scope:
    name: UI branch scope check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.ref_name == 'ui-polish' || github.ref_name == 'ui-polish-refresh' || github.head_ref == 'ui-polish' || github.head_ref == 'ui-polish-refresh'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: git fetch origin feature-miniprogram-flow
      - run: npm run check:ui-branch-scope -- origin/feature-miniprogram-flow HEAD

  wechat-release-config:
    name: WeChat release config check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.ref_name == 'release-wechat-config' || github.head_ref == 'release-wechat-config'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run check:release-config
